export type Livro = {
  titulo: string;
  autor: string;
  genero: string;
  avaliacao: string;
  cor: string;
  simbolo: string;
  sinopse: string;
  amazonUrl?: string;
  real?: boolean;
  capaUrl?: string;
};
